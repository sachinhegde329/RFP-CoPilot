
'use client'

// This component is no longer needed as its logic has been merged
// into the main page component. It is kept to avoid file deletion issues.
export function SecurityClient({ tenantSubdomain }: { tenantSubdomain: string }) {
  return null;
}
